/**
 * Forum_postController
 *
 * @description :: Server-side logic for managing forum_posts
 * @help        :: See http://sailsjs.org/#!/documentation/concepts/Controllers
 */

module.exports = {
	find(req, res) {
		if (req.query.include) {
			
		}
		console.log(req);
	}
};

